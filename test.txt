let data = { "people": [ { "name": "John", "age": 20 }, { "name": "Jane", "age": 30 } ] };

puts(data);


let getName = fn (person) {
  return person.name;
};

let map = fn (f, arr) {
  let i = 0;
  let result = arr;

  while (i < len(arr)) {
    result[i] = f(arr[i]);
    i = i + 1;
  }
  
  return result;
};

puts(map(getName, data.people));
puts(data);